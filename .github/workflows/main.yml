name : Hello World

on : [push, pull_request]

jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps:
    #- uses: actions/checkout@v2
    - name: Run pwd
      run: pwd
    - name : Run ls -al
      run: ls -al
